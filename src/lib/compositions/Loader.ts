import type { CardanoWasm } from '$lib/types/cardano/wasm';

class Loader {
  //  private _wasm: CardanoWasm;
  //  async load() {
  //    if (this._wasm) return;
  //    /**
  //     * @private
  //     */
  //    this._wasm = await import(
  //      '$lib/../../node_modules/cardano-serialization-lib'
  //    );
  //  }
 
  //  get Cardano() {
  //    return this._wasm;
  //  }
 }
 
 export default new Loader();